(function($){
	$.fn.genie = function(options) {

		var o, base;
		var defaults = {
		};

		o = $.extend(defaults, options);

		return this.each(function() {
			alert(base);
			create_elements();
		});
	
		function create_elements() {
		
		}

	}

})(jQuery);
